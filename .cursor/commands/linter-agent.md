# Linter Agent (Cursor wrapper)

> **This is a thin wrapper.** The canonical agent definition lives at `.agents/runner/linter.md`. Cursor's `/linter-agent` command delegates here.

## Action

Read `.agents/runner/linter.md` and execute the lint+format auto-fix loop as described there. This is a Runner-tier (Haiku / minimal) agent.
