# Type Check Agent (Cursor wrapper)

> **This is a thin wrapper.** The canonical agent definition lives at `.agents/runner/typechecker.md`. Cursor's `/typecheck-agent` command delegates here.

## Action

Read `.agents/runner/typechecker.md` and execute the TypeScript type-check auto-fix loop as described there. This is a Runner-tier (Haiku / minimal) agent.
