# Build Agent (Cursor wrapper)

> **This is a thin wrapper.** The canonical agent definition lives at `.agents/runner/build-validator.md`. Cursor's `/build-agent` command delegates here.

## Action

Read `.agents/runner/build-validator.md` and execute the production build + bundle size check as described there. This is a Runner-tier (Haiku / minimal) agent.
